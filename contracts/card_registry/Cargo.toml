[package]
name = "card_registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
shared_types = { path = "../../packages/shared_types" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
